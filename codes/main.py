from mle_dr_bth import mle_dr_bth
from mle_dr_psc import mle_dr_psc
from mle_dr_opc import mle_dr_opc
from mle_dr_bad import mle_dr_bad

if __name__ == '__main__':
    # print('running mle_dr_bth, LATE')
    # mle_dr_bth(estimator='LATE')

    # print('running mle_dr_bth, MLATE')
    # mle_dr_bth(estimator='MLATE')

    # print('running mle_dr_psc, LATE')
    # mle_dr_psc(estimator='LATE')

    # print('running mle_dr_psc, MLATE')
    # mle_dr_psc(estimator='MLATE')

    # print('running mle_dr_opc, LATE')
    # mle_dr_opc(estimator='LATE')

    # print('running mle_dr_opc, MLATE')
    # mle_dr_opc(estimator='MLATE')

    # print('running mle_dr_bad, LATE')
    # mle_dr_bad(estimator='LATE')

    print('running mle_dr_bad, MLATE')
    mle_dr_bad(estimator='MLATE')
